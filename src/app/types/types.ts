export type Pocket = {
    urlKey: string,
    dayNum: number,
    pokeId: number | null
}

export type ModalData = {
    title?: string,
    message?: string,
    pokeId?: number,
    displayed: boolean
}

